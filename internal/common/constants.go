package common

const (
	// HeaderContentType is the standard Content-Type HTTP header.
	HeaderContentType = "Content-Type"
	// HeaderHXRequest is the standard HTMX request header.
	HeaderHXRequest = "HX-Request"
	// MimeHTML is the standard HTML MIME type.
	MimeHTML = "text/html"
	// MimeJSON is the standard JSON MIME type.
	MimeJSON = "application/json"
)
