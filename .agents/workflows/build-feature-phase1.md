---
description: Phase 1: Product Architecture & Planning
---
## Phase 1: Product Architecture & Planning
**Pre-condition**: Read `.agents/skills/flash-plan/SKILL.md` — execute sections: **Session Start** + **The Architect's Protocol**.

1. Create `task.md` with `# Decision Log` and `# Execution Plan`.
2. Log every decision, deletion, and tradeoff from the Architect's Protocol into `# Decision Log`.
3. Populate `# Execution Plan` with `[ ]` checkboxes for Phase 2 and Phase 3.

**Phase 1 is complete ONLY when the user approves `task.md`.**
