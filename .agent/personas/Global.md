# Global Squad Rules

All global rules are defined in `.agents/agent.yaml` (lines 79-90) and enforced via `AGENTS.md`.
This file exists for `agent-exec.sh role` compatibility only. Do not duplicate rules here.

See: `AGENTS.md` for the canonical rule set.
