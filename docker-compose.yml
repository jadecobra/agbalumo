services:
  app:
    build: .
    image: agbalumo:latest
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      - PORT=8080
    volumes:
      - ./agbalumo.db:/app/agbalumo.db
    restart: unless-stopped
