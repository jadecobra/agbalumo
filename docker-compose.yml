services:
  app:
    build: .
    image: agbalumo:latest
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./agbalumo.db:/app/agbalumo.db
    restart: unless-stopped
